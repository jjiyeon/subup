import UI from "./NavStyle";
import { Subup, Archive } from "../components/icon/IconIndex";
const Nav = () => {
  return (
    <UI.NavContainer>
      <UI.NavHeader>
        <Subup />
      </UI.NavHeader>
    </UI.NavContainer>
  );
};

export default Nav;
